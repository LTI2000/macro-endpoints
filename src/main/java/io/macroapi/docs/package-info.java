/**
 * Build-time documentation generation.
 *
 * <p>{@link io.macroapi.docs.SiteDocsGenerator} folds every registered macro through the static
 * interpreters and writes the result into the Maven site sources, so the published catalogue,
 * cost table and dependency diagrams are derived from the compositions themselves.</p>
 */
package io.macroapi.docs;
